{
  "info": {
    "_postman_id": "c0ffee00-bd-dash-team-0001",
    "name": "BD Management · 团队看板（YAML）",
    "description": "由 `postman/skeletons/bd-management/bd-dashboard-team.yaml` 生成，不要手改本 JSON。修改 YAML 后执行 `npm run pm:skeleton:compile`。\n\nController：`DashboardController`",
    "schema": "https://schema.getpostman.com/json/collection/v2.1.0/collection.json"
  },
  "variable": [
    {
      "key": "apiBase",
      "value": ""
    },
    {
      "key": "loginData",
      "value": ""
    },
    {
      "key": "accessToken",
      "value": ""
    },
    {
      "key": "managerToken",
      "value": ""
    },
    {
      "key": "pendingApprovalId",
      "value": ""
    },
    {
      "key": "okrBdUserId",
      "value": ""
    },
    {
      "key": "okrManagerUserId",
      "value": ""
    },
    {
      "key": "okrOtherRegionBdUserId",
      "value": ""
    },
    {
      "key": "krManagerToken",
      "value": ""
    },
    {
      "key": "krBdToken",
      "value": ""
    },
    {
      "key": "adminToken",
      "value": ""
    },
    {
      "key": "teamAffiliateKeyword",
      "value": ""
    }
  ],
  "event": [
    {
      "listen": "prerequest",
      "script": {
        "type": "text/javascript",
        "exec": [
          "pm.request.headers.upsert({ key: 'Accept', value: 'application/json' });",
          "if (pm.request.body && pm.request.body.mode === 'raw') {",
          "  pm.request.headers.upsert({ key: 'Content-Type', value: 'application/json' });",
          "}",
          "const referer = (pm.environment.get('referer') || '').trim();",
          "if (referer) {",
          "  pm.request.headers.upsert({ key: 'Referer', value: referer });",
          "}",
          "const name = (pm.info && pm.info.requestName) || '';",
          "const url = pm.request.url.toString();",
          "const useKr = /KR Manager/.test(name) && !/登录/.test(name);",
          "const useAdmin = /Admin/.test(name) && !/登录/.test(name);",
          "const useManager = /Manager 登录/.test(name) && !/KR Manager 登录/.test(name)",
          "  || (!useKr && !useAdmin && /\\/leads\\/approvals|\\/api\\/v1\\/okr|\\/dashboard\\/team/i.test(url) && !/BD token|无\\s*Token|普通 BD/.test(name))",
          "  || (/\\/bd-users\\/me/.test(url) && /Manager/.test(name) && !/KR Manager/.test(name) && !/Admin/.test(name));",
          "const token = useKr",
          "  ? (pm.environment.get('krManagerToken') || pm.collectionVariables.get('krManagerToken') || '')",
          "  : useAdmin",
          "  ? (pm.environment.get('adminToken') || pm.collectionVariables.get('adminToken') || '')",
          "  : useManager",
          "  ? (pm.environment.get('managerToken') || pm.collectionVariables.get('managerToken') || '')",
          "  : (pm.environment.get('accessToken') || pm.collectionVariables.get('accessToken') || '');",
          "const needsAuth = /\\/leads\\/approvals|\\/api\\/v1\\/okr|\\/bd-users|\\/dashboard/i.test(url);",
          "const skipAuth = /无\\s*Token/.test(name);",
          "if (needsAuth && token && !skipAuth) {",
          "  pm.request.headers.upsert({ key: 'Authorization', value: 'Bearer ' + token.trim() });",
          "}"
        ]
      }
    },
    {
      "listen": "test",
      "script": {
        "type": "text/javascript",
        "exec": [
          "if (!pm.response || pm.response.code === undefined) {",
          "  const err = (pm.response && pm.response.reason && pm.response.reason().message) || '请求失败';",
          "  pm.test('请求可达（请检查 apiBase 是否为真实地址）', () => {",
          "    pm.expect.fail(err);",
          "  });",
          "  return;",
          "}",
          "const ct = (pm.response.headers.get('Content-Type') || '').toLowerCase();",
          "const text = pm.response.text() || '';",
          "if (ct.includes('text/html') || text.trimStart().startsWith('<!')) {",
          "  pm.test('响应应为 JSON API（非 HTML）', () => {",
          "    pm.expect.fail('got HTML — 请检查 apiBase 是否正确');",
          "  });",
          "  return;",
          "}",
          "try {",
          "  const body = pm.response.json();",
          "  if (body && body.ret_code === 0 && typeof body.token === 'string' && body.token) {",
          "    const n = (pm.info && pm.info.requestName) || '';",
          "    if (/Admin 登录/.test(n)) {",
          "      pm.environment.set('adminToken', body.token);",
          "      pm.collectionVariables.set('adminToken', body.token);",
          "    } else if (/KR Manager 登录/.test(n)) {",
          "      pm.environment.set('krManagerToken', body.token);",
          "      pm.collectionVariables.set('krManagerToken', body.token);",
          "    } else if (/KR BD 登录/.test(n)) {",
          "      pm.environment.set('krBdToken', body.token);",
          "      pm.collectionVariables.set('krBdToken', body.token);",
          "    } else if (/Manager 登录/.test(n)) {",
          "      pm.environment.set('managerToken', body.token);",
          "      pm.collectionVariables.set('managerToken', body.token);",
          "    } else if (/login/i.test(pm.request.url.toString())) {",
          "      pm.environment.set('accessToken', body.token);",
          "      pm.collectionVariables.set('accessToken', body.token);",
          "    }",
          "  }",
          "} catch (e) { /* ignore */ }"
        ]
      }
    }
  ],
  "item": [
    {
      "name": "00 Setup · 登录与本区 BD",
      "item": [
        {
          "name": "[ok] L-01 Manager 登录",
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const email = pm.environment.get('managerEmail');",
                  "const password = pm.environment.get('managerPassword');",
                  "const brokerId = pm.environment.get('brokerId') || '1';",
                  "const plain = JSON.stringify({ email, password, broker_id: Number(brokerId) });",
                  "pm.collectionVariables.set('loginData', JSON.stringify(plain));"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('HTTP 200', () => pm.response.to.have.status(200));",
                  "const body = pm.response.json();",
                  "pm.test('ret_code=0', () => pm.expect(body.ret_code).to.eql(0));",
                  "pm.test('token 非空', () => {",
                  "  pm.expect(body.token).to.be.a('string').and.not.eql('');",
                  "  pm.expect(body.result.token).to.eql(body.token);",
                  "});",
                  "pm.collectionVariables.set('managerToken', body.token);",
                  "pm.environment.set('managerToken', body.token);"
                ]
              }
            }
          ],
          "request": {
            "method": "POST",
            "header": [],
            "url": "{{apiBase}}/api/v1/login",
            "body": {
              "mode": "raw",
              "raw": "{\n  \"data\": {{loginData}},\n  \"broker_id\": {{brokerId}}\n}"
            }
          }
        },
        {
          "name": "[ok] L-02 BD 登录",
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const email = pm.environment.get('bdEmail');",
                  "const password = pm.environment.get('bdPassword');",
                  "const brokerId = pm.environment.get('brokerId') || '1';",
                  "const plain = JSON.stringify({ email, password, broker_id: Number(brokerId) });",
                  "pm.collectionVariables.set('loginData', JSON.stringify(plain));"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('HTTP 200', () => pm.response.to.have.status(200));",
                  "const body = pm.response.json();",
                  "pm.test('ret_code=0', () => pm.expect(body.ret_code).to.eql(0));",
                  "pm.test('token 非空', () => {",
                  "  pm.expect(body.token).to.be.a('string').and.not.eql('');",
                  "  pm.expect(body.result.token).to.eql(body.token);",
                  "});",
                  "pm.collectionVariables.set('accessToken', body.token);",
                  "pm.environment.set('accessToken', body.token);"
                ]
              }
            }
          ],
          "request": {
            "method": "POST",
            "header": [],
            "url": "{{apiBase}}/api/v1/login",
            "body": {
              "mode": "raw",
              "raw": "{\n  \"data\": {{loginData}},\n  \"broker_id\": {{brokerId}}\n}"
            }
          }
        },
        {
          "name": "[ok] L-03 KR Manager 登录",
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const email = pm.environment.get('krManagerEmail');",
                  "const password = pm.environment.get('krManagerPassword');",
                  "const brokerId = pm.environment.get('brokerId') || '1';",
                  "const plain = JSON.stringify({ email, password, broker_id: Number(brokerId) });",
                  "pm.collectionVariables.set('loginData', JSON.stringify(plain));",
                  "const _need = [\"krManagerEmail\",\"krManagerPassword\"];",
                  "const _missing = _need.filter((k) => !String(pm.environment.get(k) || pm.collectionVariables.get(k) || \"\").trim());",
                  "if (_missing.length) {",
                  "  console.log('跳过：缺少 ' + _missing.join(', '));",
                  "  if (typeof pm.execution !== \"undefined\" && pm.execution.skipRequest) {",
                  "    pm.execution.skipRequest();",
                  "  }",
                  "}"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const _need = [\"krManagerEmail\",\"krManagerPassword\"];",
                  "const _missing = _need.filter((k) => !String(pm.environment.get(k) || pm.collectionVariables.get(k) || \"\").trim());",
                  "if (_missing.length) {",
                  "  pm.test('已跳过（缺少 ' + _missing.join(', ') + '）', () => {",
                  "    pm.expect(true).to.be.true;",
                  "  });",
                  "  return;",
                  "}",
                  "pm.test('HTTP 200', () => pm.response.to.have.status(200));",
                  "const body = pm.response.json();",
                  "pm.test('ret_code=0', () => pm.expect(body.ret_code).to.eql(0));",
                  "pm.test('token 非空', () => {",
                  "  pm.expect(body.token).to.be.a('string').and.not.eql('');",
                  "  pm.expect(body.result.token).to.eql(body.token);",
                  "});",
                  "pm.collectionVariables.set('krManagerToken', body.token);",
                  "pm.environment.set('krManagerToken', body.token);"
                ]
              }
            }
          ],
          "request": {
            "method": "POST",
            "header": [],
            "url": "{{apiBase}}/api/v1/login",
            "body": {
              "mode": "raw",
              "raw": "{\n  \"data\": {{loginData}},\n  \"broker_id\": {{brokerId}}\n}"
            }
          }
        },
        {
          "name": "[ok] L-04 KR BD 登录",
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const email = pm.environment.get('krBdEmail');",
                  "const password = pm.environment.get('krBdPassword');",
                  "const brokerId = pm.environment.get('brokerId') || '1';",
                  "const plain = JSON.stringify({ email, password, broker_id: Number(brokerId) });",
                  "pm.collectionVariables.set('loginData', JSON.stringify(plain));",
                  "const _need = [\"krBdEmail\",\"krBdPassword\"];",
                  "const _missing = _need.filter((k) => !String(pm.environment.get(k) || pm.collectionVariables.get(k) || \"\").trim());",
                  "if (_missing.length) {",
                  "  console.log('跳过：缺少 ' + _missing.join(', '));",
                  "  if (typeof pm.execution !== \"undefined\" && pm.execution.skipRequest) {",
                  "    pm.execution.skipRequest();",
                  "  }",
                  "}"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const _need = [\"krBdEmail\",\"krBdPassword\"];",
                  "const _missing = _need.filter((k) => !String(pm.environment.get(k) || pm.collectionVariables.get(k) || \"\").trim());",
                  "if (_missing.length) {",
                  "  pm.test('已跳过（缺少 ' + _missing.join(', ') + '）', () => {",
                  "    pm.expect(true).to.be.true;",
                  "  });",
                  "  return;",
                  "}",
                  "pm.test('HTTP 200', () => pm.response.to.have.status(200));",
                  "const body = pm.response.json();",
                  "pm.test('ret_code=0', () => pm.expect(body.ret_code).to.eql(0));",
                  "pm.test('token 非空', () => {",
                  "  pm.expect(body.token).to.be.a('string').and.not.eql('');",
                  "  pm.expect(body.result.token).to.eql(body.token);",
                  "});",
                  "pm.collectionVariables.set('krBdToken', body.token);",
                  "pm.environment.set('krBdToken', body.token);"
                ]
              }
            }
          ],
          "request": {
            "method": "POST",
            "header": [],
            "url": "{{apiBase}}/api/v1/login",
            "body": {
              "mode": "raw",
              "raw": "{\n  \"data\": {{loginData}},\n  \"broker_id\": {{brokerId}}\n}"
            }
          }
        },
        {
          "name": "[ok] L-05 Admin 登录",
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const email = pm.environment.get('adminEmail');",
                  "const password = pm.environment.get('adminPassword');",
                  "const brokerId = pm.environment.get('brokerId') || '1';",
                  "const plain = JSON.stringify({ email, password, broker_id: Number(brokerId) });",
                  "pm.collectionVariables.set('loginData', JSON.stringify(plain));",
                  "const _need = [\"adminEmail\",\"adminPassword\"];",
                  "const _missing = _need.filter((k) => !String(pm.environment.get(k) || pm.collectionVariables.get(k) || \"\").trim());",
                  "if (_missing.length) {",
                  "  console.log('跳过：缺少 ' + _missing.join(', '));",
                  "  if (typeof pm.execution !== \"undefined\" && pm.execution.skipRequest) {",
                  "    pm.execution.skipRequest();",
                  "  }",
                  "}"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const _need = [\"adminEmail\",\"adminPassword\"];",
                  "const _missing = _need.filter((k) => !String(pm.environment.get(k) || pm.collectionVariables.get(k) || \"\").trim());",
                  "if (_missing.length) {",
                  "  pm.test('已跳过（缺少 ' + _missing.join(', ') + '）', () => {",
                  "    pm.expect(true).to.be.true;",
                  "  });",
                  "  return;",
                  "}",
                  "pm.test('HTTP 200', () => pm.response.to.have.status(200));",
                  "const body = pm.response.json();",
                  "pm.test('ret_code=0', () => pm.expect(body.ret_code).to.eql(0));",
                  "pm.test('token 非空', () => {",
                  "  pm.expect(body.token).to.be.a('string').and.not.eql('');",
                  "  pm.expect(body.result.token).to.eql(body.token);",
                  "});",
                  "pm.collectionVariables.set('adminToken', body.token);",
                  "pm.environment.set('adminToken', body.token);"
                ]
              }
            }
          ],
          "request": {
            "method": "POST",
            "header": [],
            "url": "{{apiBase}}/api/v1/login",
            "body": {
              "mode": "raw",
              "raw": "{\n  \"data\": {{loginData}},\n  \"broker_id\": {{brokerId}}\n}"
            }
          }
        },
        {
          "name": "[ok] S-03 GET /bd-users/me（KR BD）解析 okrOtherRegionBdUserId",
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const _tok = (pm.environment.get('krBdToken') || pm.collectionVariables.get('krBdToken') || '').trim();",
                  "if (_tok) {",
                  "  pm.request.headers.upsert({ key: 'Authorization', value: 'Bearer ' + _tok });",
                  "}",
                  "const _need = [\"krBdToken\"];",
                  "const _missing = _need.filter((k) => !String(pm.environment.get(k) || pm.collectionVariables.get(k) || \"\").trim());",
                  "if (_missing.length) {",
                  "  console.log('跳过：缺少 ' + _missing.join(', '));",
                  "  if (typeof pm.execution !== \"undefined\" && pm.execution.skipRequest) {",
                  "    pm.execution.skipRequest();",
                  "  }",
                  "}"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const _need = [\"krBdToken\"];",
                  "const _missing = _need.filter((k) => !String(pm.environment.get(k) || pm.collectionVariables.get(k) || \"\").trim());",
                  "if (_missing.length) {",
                  "  pm.test('已跳过（缺少 ' + _missing.join(', ') + '）', () => {",
                  "    pm.expect(true).to.be.true;",
                  "  });",
                  "  return;",
                  "}",
                  "pm.test('HTTP 200', () => pm.response.to.have.status(200));",
                  "const body = pm.response.json();",
                  "pm.test('ret_code=0', () => pm.expect(body.ret_code).to.eql(0));",
                  "const r = body.result || {};",
                  "if (r?.id != null && r?.id !== '') {",
                  "  const _v = String(r?.id);",
                  "  const _env = (pm.environment.get(\"okrOtherRegionBdUserId\") || '').toString().trim();",
                  "  const _col = (pm.collectionVariables.get(\"okrOtherRegionBdUserId\") || '').toString().trim();",
                  "  if (!_col) {",
                  "    pm.collectionVariables.set(\"okrOtherRegionBdUserId\", _v);",
                  "  }",
                  "  if (!_env) {",
                  "    pm.environment.set(\"okrOtherRegionBdUserId\", _v);",
                  "  }",
                  "}"
                ]
              }
            }
          ],
          "request": {
            "method": "GET",
            "header": [],
            "url": "{{apiBase}}/api/v1/bd-users/me"
          }
        },
        {
          "name": "[ok] S-01 GET /bd-users/me（BD）解析 okrBdUserId",
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const _tok = (pm.environment.get('accessToken') || pm.collectionVariables.get('accessToken') || '').trim();",
                  "if (_tok) {",
                  "  pm.request.headers.upsert({ key: 'Authorization', value: 'Bearer ' + _tok });",
                  "}"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('HTTP 200', () => pm.response.to.have.status(200));",
                  "const body = pm.response.json();",
                  "pm.test('ret_code=0', () => pm.expect(body.ret_code).to.eql(0));",
                  "const r = body.result || {};",
                  "if (r?.id != null && r?.id !== '') {",
                  "  const _v = String(r?.id);",
                  "  const _env = (pm.environment.get(\"okrBdUserId\") || '').toString().trim();",
                  "  const _col = (pm.collectionVariables.get(\"okrBdUserId\") || '').toString().trim();",
                  "  if (!_col) {",
                  "    pm.collectionVariables.set(\"okrBdUserId\", _v);",
                  "  }",
                  "  if (!_env) {",
                  "    pm.environment.set(\"okrBdUserId\", _v);",
                  "  }",
                  "}"
                ]
              }
            }
          ],
          "request": {
            "method": "GET",
            "header": [],
            "url": "{{apiBase}}/api/v1/bd-users/me"
          }
        },
        {
          "name": "[ok] S-02 GET /bd-users/me（Manager）解析 okrManagerUserId",
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const _tok = (pm.environment.get('managerToken') || pm.collectionVariables.get('managerToken') || '').trim();",
                  "if (_tok) {",
                  "  pm.request.headers.upsert({ key: 'Authorization', value: 'Bearer ' + _tok });",
                  "}"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('HTTP 200', () => pm.response.to.have.status(200));",
                  "const body = pm.response.json();",
                  "pm.test('ret_code=0', () => pm.expect(body.ret_code).to.eql(0));",
                  "const r = body.result || {};",
                  "if (r?.id != null && r?.id !== '') {",
                  "  const _v = String(r?.id);",
                  "  const _env = (pm.environment.get(\"okrManagerUserId\") || '').toString().trim();",
                  "  const _col = (pm.collectionVariables.get(\"okrManagerUserId\") || '').toString().trim();",
                  "  if (!_col) {",
                  "    pm.collectionVariables.set(\"okrManagerUserId\", _v);",
                  "  }",
                  "  if (!_env) {",
                  "    pm.environment.set(\"okrManagerUserId\", _v);",
                  "  }",
                  "}"
                ]
              }
            }
          ],
          "request": {
            "method": "GET",
            "header": [],
            "url": "{{apiBase}}/api/v1/bd-users/me"
          }
        }
      ]
    },
    {
      "name": "A · AC-TM-01 GET /dashboard/team/okr-progress",
      "item": [
        {
          "name": "[ok] AC-TM-01 本区 Manager 默认季",
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const _tok = (pm.environment.get('managerToken') || pm.collectionVariables.get('managerToken') || '').trim();",
                  "if (_tok) {",
                  "  pm.request.headers.upsert({ key: 'Authorization', value: 'Bearer ' + _tok });",
                  "}"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('HTTP 200', () => pm.response.to.have.status(200));",
                  "const body = pm.response.json();",
                  "pm.test('ret_code=0', () => pm.expect(body.ret_code).to.eql(0));",
                  "const r = body.result || {};",
                  "pm.test('分页结构', () => {",
                  "  pm.expect(r).to.include.keys(\"period\", \"period_key\", \"begin\", \"end\", \"list\");",
                  "  pm.expect(r.list).to.be.an('array');",
                  "});",
                  "{",
                  "  const kind = \"quarter\";",
                  "  const now = new Date();",
                  "  const y = now.getUTCFullYear();",
                  "  const m = now.getUTCMonth();",
                  "  const d = now.getUTCDate();",
                  "  const pad = (n) => String(n).padStart(2, \"0\");",
                  "  const ymd = (yy, mm, dd) => yy + \"-\" + pad(mm) + \"-\" + pad(dd);",
                  "  const lastDay = (yy, mm) => new Date(Date.UTC(yy, mm, 0)).getUTCDate();",
                  "  let period = kind;",
                  "  let periodKey;",
                  "  let begin;",
                  "  let end;",
                  "  if (kind === \"month\") {",
                  "    periodKey = y + \"-\" + pad(m + 1);",
                  "    begin = ymd(y, m + 1, 1);",
                  "    end = ymd(y, m + 1, lastDay(y, m + 1));",
                  "  } else if (kind === \"week\") {",
                  "    const utc = new Date(Date.UTC(y, m, d));",
                  "    const dow = utc.getUTCDay() || 7;",
                  "    const monday = new Date(utc);",
                  "    monday.setUTCDate(utc.getUTCDate() - (dow - 1));",
                  "    const sunday = new Date(monday);",
                  "    sunday.setUTCDate(monday.getUTCDate() + 6);",
                  "    const thu = new Date(monday);",
                  "    thu.setUTCDate(monday.getUTCDate() + 3);",
                  "    const wy = thu.getUTCFullYear();",
                  "    const jan4 = new Date(Date.UTC(wy, 0, 4));",
                  "    const jan4Dow = jan4.getUTCDay() || 7;",
                  "    const w1 = new Date(jan4);",
                  "    w1.setUTCDate(jan4.getUTCDate() - (jan4Dow - 1));",
                  "    const week = 1 + Math.round((monday - w1) / 604800000);",
                  "    periodKey = wy + \"-W\" + pad(week);",
                  "    begin = ymd(monday.getUTCFullYear(), monday.getUTCMonth() + 1, monday.getUTCDate());",
                  "    end = ymd(sunday.getUTCFullYear(), sunday.getUTCMonth() + 1, sunday.getUTCDate());",
                  "  } else {",
                  "    const q = Math.floor(m / 3) + 1;",
                  "    const bm = (q - 1) * 3 + 1;",
                  "    periodKey = y + \"Q\" + q;",
                  "    begin = ymd(y, bm, 1);",
                  "    end = ymd(y, bm + 2, lastDay(y, bm + 2));",
                  "  }",
                  "  pm.test('当前 UTC ' + kind + ' 闭区间', () => {",
                  "    pm.expect(r.period).to.eql(period);",
                  "    pm.expect(r.period_key).to.eql(periodKey);",
                  "    pm.expect(r.begin).to.eql(begin);",
                  "    pm.expect(r.end).to.eql(end);",
                  "  });",
                  "}",
                  "pm.test('result.list[] 含 bd_user_id/name/manager_name/items/overall_ratio', () => {",
                  "  pm.expect(r.list).to.be.an('array');",
                  "  (r.list || []).forEach((it) => {",
                  "    pm.expect(it).to.include.keys(\"bd_user_id\", \"name\", \"manager_name\", \"items\", \"overall_ratio\");",
                  "  });",
                  "});",
                  "pm.test('result.list[] 五卡 signup/eftd/daut/volume/new_affiliates', () => {",
                  "  const metrics = ['signup', 'eftd', 'daut', 'volume', 'new_affiliates'];",
                  "  (r.list || []).forEach((row) => {",
                  "    pm.expect(row.items).to.be.an('array').and.have.lengthOf(5);",
                  "    pm.expect(row.items.map((it) => it.metric)).to.eql(metrics);",
                  "    row.items.forEach((it) => {",
                  "      if (it.target == null) {",
                  "        pm.expect(it.ratio).to.eql(null);",
                  "      }",
                  "    });",
                  "  });",
                  "});",
                  "pm.test('result.list[].overall_ratio 在 0–1 或 null', () => {",
                  "  (r.list || []).forEach((row) => {",
                  "    const v = row.overall_ratio;",
                  "    if (v == null) { return; }",
                  "    pm.expect(Number(v)).to.be.at.least(0);",
                  "    pm.expect(Number(v)).to.be.at.most(1);",
                  "  });",
                  "});",
                  "pm.collectionVariables.set(\"teamOkrQuarterList\", JSON.stringify(r.list == null ? null : r.list));"
                ]
              }
            }
          ],
          "request": {
            "method": "GET",
            "header": [],
            "url": "{{apiBase}}/api/v1/dashboard/team/okr-progress"
          }
        },
        {
          "name": "[ok] AC-TM-01b period=month 当前 UTC 月",
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const _tok = (pm.environment.get('managerToken') || pm.collectionVariables.get('managerToken') || '').trim();",
                  "if (_tok) {",
                  "  pm.request.headers.upsert({ key: 'Authorization', value: 'Bearer ' + _tok });",
                  "}"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('HTTP 200', () => pm.response.to.have.status(200));",
                  "const body = pm.response.json();",
                  "pm.test('ret_code=0', () => pm.expect(body.ret_code).to.eql(0));",
                  "const r = body.result || {};",
                  "{",
                  "  const kind = \"month\";",
                  "  const now = new Date();",
                  "  const y = now.getUTCFullYear();",
                  "  const m = now.getUTCMonth();",
                  "  const d = now.getUTCDate();",
                  "  const pad = (n) => String(n).padStart(2, \"0\");",
                  "  const ymd = (yy, mm, dd) => yy + \"-\" + pad(mm) + \"-\" + pad(dd);",
                  "  const lastDay = (yy, mm) => new Date(Date.UTC(yy, mm, 0)).getUTCDate();",
                  "  let period = kind;",
                  "  let periodKey;",
                  "  let begin;",
                  "  let end;",
                  "  if (kind === \"month\") {",
                  "    periodKey = y + \"-\" + pad(m + 1);",
                  "    begin = ymd(y, m + 1, 1);",
                  "    end = ymd(y, m + 1, lastDay(y, m + 1));",
                  "  } else if (kind === \"week\") {",
                  "    const utc = new Date(Date.UTC(y, m, d));",
                  "    const dow = utc.getUTCDay() || 7;",
                  "    const monday = new Date(utc);",
                  "    monday.setUTCDate(utc.getUTCDate() - (dow - 1));",
                  "    const sunday = new Date(monday);",
                  "    sunday.setUTCDate(monday.getUTCDate() + 6);",
                  "    const thu = new Date(monday);",
                  "    thu.setUTCDate(monday.getUTCDate() + 3);",
                  "    const wy = thu.getUTCFullYear();",
                  "    const jan4 = new Date(Date.UTC(wy, 0, 4));",
                  "    const jan4Dow = jan4.getUTCDay() || 7;",
                  "    const w1 = new Date(jan4);",
                  "    w1.setUTCDate(jan4.getUTCDate() - (jan4Dow - 1));",
                  "    const week = 1 + Math.round((monday - w1) / 604800000);",
                  "    periodKey = wy + \"-W\" + pad(week);",
                  "    begin = ymd(monday.getUTCFullYear(), monday.getUTCMonth() + 1, monday.getUTCDate());",
                  "    end = ymd(sunday.getUTCFullYear(), sunday.getUTCMonth() + 1, sunday.getUTCDate());",
                  "  } else {",
                  "    const q = Math.floor(m / 3) + 1;",
                  "    const bm = (q - 1) * 3 + 1;",
                  "    periodKey = y + \"Q\" + q;",
                  "    begin = ymd(y, bm, 1);",
                  "    end = ymd(y, bm + 2, lastDay(y, bm + 2));",
                  "  }",
                  "  pm.test('当前 UTC ' + kind + ' 闭区间', () => {",
                  "    pm.expect(r.period).to.eql(period);",
                  "    pm.expect(r.period_key).to.eql(periodKey);",
                  "    pm.expect(r.begin).to.eql(begin);",
                  "    pm.expect(r.end).to.eql(end);",
                  "  });",
                  "}",
                  "pm.test('result.list[] 五卡 signup/eftd/daut/volume/new_affiliates', () => {",
                  "  const metrics = ['signup', 'eftd', 'daut', 'volume', 'new_affiliates'];",
                  "  (r.list || []).forEach((row) => {",
                  "    pm.expect(row.items).to.be.an('array').and.have.lengthOf(5);",
                  "    pm.expect(row.items.map((it) => it.metric)).to.eql(metrics);",
                  "    row.items.forEach((it) => {",
                  "      if (it.target == null) {",
                  "        pm.expect(it.ratio).to.eql(null);",
                  "      }",
                  "    });",
                  "  });",
                  "});"
                ]
              }
            }
          ],
          "request": {
            "method": "GET",
            "header": [],
            "url": "{{apiBase}}/api/v1/dashboard/team/okr-progress?period=month"
          }
        },
        {
          "name": "[ok] AC-TM-01c period=week 当前 UTC ISO 周",
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const _tok = (pm.environment.get('managerToken') || pm.collectionVariables.get('managerToken') || '').trim();",
                  "if (_tok) {",
                  "  pm.request.headers.upsert({ key: 'Authorization', value: 'Bearer ' + _tok });",
                  "}"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('HTTP 200', () => pm.response.to.have.status(200));",
                  "const body = pm.response.json();",
                  "pm.test('ret_code=0', () => pm.expect(body.ret_code).to.eql(0));",
                  "const r = body.result || {};",
                  "{",
                  "  const kind = \"week\";",
                  "  const now = new Date();",
                  "  const y = now.getUTCFullYear();",
                  "  const m = now.getUTCMonth();",
                  "  const d = now.getUTCDate();",
                  "  const pad = (n) => String(n).padStart(2, \"0\");",
                  "  const ymd = (yy, mm, dd) => yy + \"-\" + pad(mm) + \"-\" + pad(dd);",
                  "  const lastDay = (yy, mm) => new Date(Date.UTC(yy, mm, 0)).getUTCDate();",
                  "  let period = kind;",
                  "  let periodKey;",
                  "  let begin;",
                  "  let end;",
                  "  if (kind === \"month\") {",
                  "    periodKey = y + \"-\" + pad(m + 1);",
                  "    begin = ymd(y, m + 1, 1);",
                  "    end = ymd(y, m + 1, lastDay(y, m + 1));",
                  "  } else if (kind === \"week\") {",
                  "    const utc = new Date(Date.UTC(y, m, d));",
                  "    const dow = utc.getUTCDay() || 7;",
                  "    const monday = new Date(utc);",
                  "    monday.setUTCDate(utc.getUTCDate() - (dow - 1));",
                  "    const sunday = new Date(monday);",
                  "    sunday.setUTCDate(monday.getUTCDate() + 6);",
                  "    const thu = new Date(monday);",
                  "    thu.setUTCDate(monday.getUTCDate() + 3);",
                  "    const wy = thu.getUTCFullYear();",
                  "    const jan4 = new Date(Date.UTC(wy, 0, 4));",
                  "    const jan4Dow = jan4.getUTCDay() || 7;",
                  "    const w1 = new Date(jan4);",
                  "    w1.setUTCDate(jan4.getUTCDate() - (jan4Dow - 1));",
                  "    const week = 1 + Math.round((monday - w1) / 604800000);",
                  "    periodKey = wy + \"-W\" + pad(week);",
                  "    begin = ymd(monday.getUTCFullYear(), monday.getUTCMonth() + 1, monday.getUTCDate());",
                  "    end = ymd(sunday.getUTCFullYear(), sunday.getUTCMonth() + 1, sunday.getUTCDate());",
                  "  } else {",
                  "    const q = Math.floor(m / 3) + 1;",
                  "    const bm = (q - 1) * 3 + 1;",
                  "    periodKey = y + \"Q\" + q;",
                  "    begin = ymd(y, bm, 1);",
                  "    end = ymd(y, bm + 2, lastDay(y, bm + 2));",
                  "  }",
                  "  pm.test('当前 UTC ' + kind + ' 闭区间', () => {",
                  "    pm.expect(r.period).to.eql(period);",
                  "    pm.expect(r.period_key).to.eql(periodKey);",
                  "    pm.expect(r.begin).to.eql(begin);",
                  "    pm.expect(r.end).to.eql(end);",
                  "  });",
                  "}",
                  "pm.test('result.list[] 五卡 signup/eftd/daut/volume/new_affiliates', () => {",
                  "  const metrics = ['signup', 'eftd', 'daut', 'volume', 'new_affiliates'];",
                  "  (r.list || []).forEach((row) => {",
                  "    pm.expect(row.items).to.be.an('array').and.have.lengthOf(5);",
                  "    pm.expect(row.items.map((it) => it.metric)).to.eql(metrics);",
                  "    row.items.forEach((it) => {",
                  "      if (it.target == null) {",
                  "        pm.expect(it.ratio).to.eql(null);",
                  "      }",
                  "    });",
                  "  });",
                  "});",
                  "pm.collectionVariables.set(\"teamOkrWeekList\", JSON.stringify(r.list == null ? null : r.list));"
                ]
              }
            }
          ],
          "request": {
            "method": "GET",
            "header": [],
            "url": "{{apiBase}}/api/v1/dashboard/team/okr-progress?period=week"
          }
        },
        {
          "name": "[ok] AC-TM-01d KR Manager 不含本区 BD",
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const _tok = (pm.environment.get('krManagerToken') || pm.collectionVariables.get('krManagerToken') || '').trim();",
                  "if (_tok) {",
                  "  pm.request.headers.upsert({ key: 'Authorization', value: 'Bearer ' + _tok });",
                  "}",
                  "const _need = [\"krManagerToken\",\"okrBdUserId\"];",
                  "const _missing = _need.filter((k) => !String(pm.environment.get(k) || pm.collectionVariables.get(k) || \"\").trim());",
                  "if (_missing.length) {",
                  "  console.log('跳过：缺少 ' + _missing.join(', '));",
                  "  if (typeof pm.execution !== \"undefined\" && pm.execution.skipRequest) {",
                  "    pm.execution.skipRequest();",
                  "  }",
                  "}"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const _need = [\"krManagerToken\",\"okrBdUserId\"];",
                  "const _missing = _need.filter((k) => !String(pm.environment.get(k) || pm.collectionVariables.get(k) || \"\").trim());",
                  "if (_missing.length) {",
                  "  pm.test('已跳过（缺少 ' + _missing.join(', ') + '）', () => {",
                  "    pm.expect(true).to.be.true;",
                  "  });",
                  "  return;",
                  "}",
                  "pm.test('HTTP 200', () => pm.response.to.have.status(200));",
                  "const body = pm.response.json();",
                  "pm.test('ret_code=0', () => pm.expect(body.ret_code).to.eql(0));",
                  "const r = body.result || {};",
                  "pm.test('result.list 不含 bd_user_id=' + (pm.environment.get(\"okrBdUserId\") || pm.collectionVariables.get(\"okrBdUserId\") || ''), () => {",
                  "  const nid = Number((pm.environment.get(\"okrBdUserId\") || pm.collectionVariables.get(\"okrBdUserId\") || ''));",
                  "  const hit = (r.list || []).some((it) => Number(it.bd_user_id) === nid);",
                  "  pm.expect(hit).to.eql(false);",
                  "});"
                ]
              }
            }
          ],
          "request": {
            "method": "GET",
            "header": [],
            "url": "{{apiBase}}/api/v1/dashboard/team/okr-progress"
          }
        },
        {
          "name": "[err] AC-TM-01e ADMIN 传 manager_id 与 region 不一致",
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const _tok = (pm.environment.get('adminToken') || pm.collectionVariables.get('adminToken') || '').trim();",
                  "if (_tok) {",
                  "  pm.request.headers.upsert({ key: 'Authorization', value: 'Bearer ' + _tok });",
                  "}",
                  "const _need = [\"adminToken\",\"okrManagerUserId\"];",
                  "const _missing = _need.filter((k) => !String(pm.environment.get(k) || pm.collectionVariables.get(k) || \"\").trim());",
                  "if (_missing.length) {",
                  "  console.log('跳过：缺少 ' + _missing.join(', '));",
                  "  if (typeof pm.execution !== \"undefined\" && pm.execution.skipRequest) {",
                  "    pm.execution.skipRequest();",
                  "  }",
                  "}"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const _need = [\"adminToken\",\"okrManagerUserId\"];",
                  "const _missing = _need.filter((k) => !String(pm.environment.get(k) || pm.collectionVariables.get(k) || \"\").trim());",
                  "if (_missing.length) {",
                  "  pm.test('已跳过（缺少 ' + _missing.join(', ') + '）', () => {",
                  "    pm.expect(true).to.be.true;",
                  "  });",
                  "  return;",
                  "}",
                  "pm.test('HTTP 200', () => pm.response.to.have.status(200));",
                  "const body = pm.response.json();",
                  "pm.test('ret_code=20300', () => pm.expect(body.ret_code).to.eql(20300));"
                ]
              }
            }
          ],
          "request": {
            "method": "GET",
            "header": [],
            "url": "{{apiBase}}/api/v1/dashboard/team/okr-progress?manager_id={{okrManagerUserId}}&region=kr"
          }
        },
        {
          "name": "[err] AC-TM-01f ADMIN 传 manager_id 非 Manager",
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const _tok = (pm.environment.get('adminToken') || pm.collectionVariables.get('adminToken') || '').trim();",
                  "if (_tok) {",
                  "  pm.request.headers.upsert({ key: 'Authorization', value: 'Bearer ' + _tok });",
                  "}",
                  "const _need = [\"adminToken\",\"okrBdUserId\"];",
                  "const _missing = _need.filter((k) => !String(pm.environment.get(k) || pm.collectionVariables.get(k) || \"\").trim());",
                  "if (_missing.length) {",
                  "  console.log('跳过：缺少 ' + _missing.join(', '));",
                  "  if (typeof pm.execution !== \"undefined\" && pm.execution.skipRequest) {",
                  "    pm.execution.skipRequest();",
                  "  }",
                  "}"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const _need = [\"adminToken\",\"okrBdUserId\"];",
                  "const _missing = _need.filter((k) => !String(pm.environment.get(k) || pm.collectionVariables.get(k) || \"\").trim());",
                  "if (_missing.length) {",
                  "  pm.test('已跳过（缺少 ' + _missing.join(', ') + '）', () => {",
                  "    pm.expect(true).to.be.true;",
                  "  });",
                  "  return;",
                  "}",
                  "pm.test('HTTP 200', () => pm.response.to.have.status(200));",
                  "const body = pm.response.json();",
                  "pm.test('ret_code=10001', () => pm.expect(body.ret_code).to.eql(10001));"
                ]
              }
            }
          ],
          "request": {
            "method": "GET",
            "header": [],
            "url": "{{apiBase}}/api/v1/dashboard/team/okr-progress?manager_id={{okrBdUserId}}"
          }
        },
        {
          "name": "[err] AC-TM-01g ADMIN 传 manager_id 不存在",
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const _tok = (pm.environment.get('adminToken') || pm.collectionVariables.get('adminToken') || '').trim();",
                  "if (_tok) {",
                  "  pm.request.headers.upsert({ key: 'Authorization', value: 'Bearer ' + _tok });",
                  "}",
                  "const _need = [\"adminToken\"];",
                  "const _missing = _need.filter((k) => !String(pm.environment.get(k) || pm.collectionVariables.get(k) || \"\").trim());",
                  "if (_missing.length) {",
                  "  console.log('跳过：缺少 ' + _missing.join(', '));",
                  "  if (typeof pm.execution !== \"undefined\" && pm.execution.skipRequest) {",
                  "    pm.execution.skipRequest();",
                  "  }",
                  "}"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const _need = [\"adminToken\"];",
                  "const _missing = _need.filter((k) => !String(pm.environment.get(k) || pm.collectionVariables.get(k) || \"\").trim());",
                  "if (_missing.length) {",
                  "  pm.test('已跳过（缺少 ' + _missing.join(', ') + '）', () => {",
                  "    pm.expect(true).to.be.true;",
                  "  });",
                  "  return;",
                  "}",
                  "pm.test('HTTP 200', () => pm.response.to.have.status(200));",
                  "const body = pm.response.json();",
                  "pm.test('ret_code=10001', () => pm.expect(body.ret_code).to.eql(10001));"
                ]
              }
            }
          ],
          "request": {
            "method": "GET",
            "header": [],
            "url": "{{apiBase}}/api/v1/dashboard/team/okr-progress?manager_id=999999999"
          }
        },
        {
          "name": "[ok] AC-TM-01h ADMIN 传合法 manager_id",
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const _tok = (pm.environment.get('adminToken') || pm.collectionVariables.get('adminToken') || '').trim();",
                  "if (_tok) {",
                  "  pm.request.headers.upsert({ key: 'Authorization', value: 'Bearer ' + _tok });",
                  "}",
                  "const _need = [\"adminToken\",\"okrManagerUserId\"];",
                  "const _missing = _need.filter((k) => !String(pm.environment.get(k) || pm.collectionVariables.get(k) || \"\").trim());",
                  "if (_missing.length) {",
                  "  console.log('跳过：缺少 ' + _missing.join(', '));",
                  "  if (typeof pm.execution !== \"undefined\" && pm.execution.skipRequest) {",
                  "    pm.execution.skipRequest();",
                  "  }",
                  "}"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const _need = [\"adminToken\",\"okrManagerUserId\"];",
                  "const _missing = _need.filter((k) => !String(pm.environment.get(k) || pm.collectionVariables.get(k) || \"\").trim());",
                  "if (_missing.length) {",
                  "  pm.test('已跳过（缺少 ' + _missing.join(', ') + '）', () => {",
                  "    pm.expect(true).to.be.true;",
                  "  });",
                  "  return;",
                  "}",
                  "pm.test('HTTP 200', () => pm.response.to.have.status(200));",
                  "const body = pm.response.json();",
                  "pm.test('ret_code=0', () => pm.expect(body.ret_code).to.eql(0));",
                  "const r = body.result || {};",
                  "pm.test('result.list 为数组', () => pm.expect(r.list).to.be.an('array'));",
                  "pm.test('result.list[] 五卡 signup/eftd/daut/volume/new_affiliates', () => {",
                  "  const metrics = ['signup', 'eftd', 'daut', 'volume', 'new_affiliates'];",
                  "  (r.list || []).forEach((row) => {",
                  "    pm.expect(row.items).to.be.an('array').and.have.lengthOf(5);",
                  "    pm.expect(row.items.map((it) => it.metric)).to.eql(metrics);",
                  "    row.items.forEach((it) => {",
                  "      if (it.target == null) {",
                  "        pm.expect(it.ratio).to.eql(null);",
                  "      }",
                  "    });",
                  "  });",
                  "});"
                ]
              }
            }
          ],
          "request": {
            "method": "GET",
            "header": [],
            "url": "{{apiBase}}/api/v1/dashboard/team/okr-progress?manager_id={{okrManagerUserId}}"
          }
        },
        {
          "name": "[ok] AC-TM-01i ADMIN 传 region=non_kr",
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const _tok = (pm.environment.get('adminToken') || pm.collectionVariables.get('adminToken') || '').trim();",
                  "if (_tok) {",
                  "  pm.request.headers.upsert({ key: 'Authorization', value: 'Bearer ' + _tok });",
                  "}",
                  "const _need = [\"adminToken\"];",
                  "const _missing = _need.filter((k) => !String(pm.environment.get(k) || pm.collectionVariables.get(k) || \"\").trim());",
                  "if (_missing.length) {",
                  "  console.log('跳过：缺少 ' + _missing.join(', '));",
                  "  if (typeof pm.execution !== \"undefined\" && pm.execution.skipRequest) {",
                  "    pm.execution.skipRequest();",
                  "  }",
                  "}"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const _need = [\"adminToken\"];",
                  "const _missing = _need.filter((k) => !String(pm.environment.get(k) || pm.collectionVariables.get(k) || \"\").trim());",
                  "if (_missing.length) {",
                  "  pm.test('已跳过（缺少 ' + _missing.join(', ') + '）', () => {",
                  "    pm.expect(true).to.be.true;",
                  "  });",
                  "  return;",
                  "}",
                  "pm.test('HTTP 200', () => pm.response.to.have.status(200));",
                  "const body = pm.response.json();",
                  "pm.test('ret_code=0', () => pm.expect(body.ret_code).to.eql(0));",
                  "const r = body.result || {};",
                  "pm.test('result.list 为数组', () => pm.expect(r.list).to.be.an('array'));"
                ]
              }
            }
          ],
          "request": {
            "method": "GET",
            "header": [],
            "url": "{{apiBase}}/api/v1/dashboard/team/okr-progress?region=non_kr"
          }
        },
        {
          "name": "[ok] AC-TM-01j ADMIN 不传 region 全区域",
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const _tok = (pm.environment.get('adminToken') || pm.collectionVariables.get('adminToken') || '').trim();",
                  "if (_tok) {",
                  "  pm.request.headers.upsert({ key: 'Authorization', value: 'Bearer ' + _tok });",
                  "}",
                  "const _need = [\"adminToken\"];",
                  "const _missing = _need.filter((k) => !String(pm.environment.get(k) || pm.collectionVariables.get(k) || \"\").trim());",
                  "if (_missing.length) {",
                  "  console.log('跳过：缺少 ' + _missing.join(', '));",
                  "  if (typeof pm.execution !== \"undefined\" && pm.execution.skipRequest) {",
                  "    pm.execution.skipRequest();",
                  "  }",
                  "}"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const _need = [\"adminToken\"];",
                  "const _missing = _need.filter((k) => !String(pm.environment.get(k) || pm.collectionVariables.get(k) || \"\").trim());",
                  "if (_missing.length) {",
                  "  pm.test('已跳过（缺少 ' + _missing.join(', ') + '）', () => {",
                  "    pm.expect(true).to.be.true;",
                  "  });",
                  "  return;",
                  "}",
                  "pm.test('HTTP 200', () => pm.response.to.have.status(200));",
                  "const body = pm.response.json();",
                  "pm.test('ret_code=0', () => pm.expect(body.ret_code).to.eql(0));",
                  "const r = body.result || {};",
                  "pm.test('分页结构', () => {",
                  "  pm.expect(r).to.include.keys(\"period\", \"period_key\", \"begin\", \"end\", \"list\");",
                  "  pm.expect(r.list).to.be.an('array');",
                  "});"
                ]
              }
            }
          ],
          "request": {
            "method": "GET",
            "header": [],
            "url": "{{apiBase}}/api/v1/dashboard/team/okr-progress"
          }
        },
        {
          "name": "[err] AC-TM-01k Manager 传他人 manager_id",
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const _tok = (pm.environment.get('managerToken') || pm.collectionVariables.get('managerToken') || '').trim();",
                  "if (_tok) {",
                  "  pm.request.headers.upsert({ key: 'Authorization', value: 'Bearer ' + _tok });",
                  "}",
                  "const _need = [\"okrOtherRegionBdUserId\"];",
                  "const _missing = _need.filter((k) => !String(pm.environment.get(k) || pm.collectionVariables.get(k) || \"\").trim());",
                  "if (_missing.length) {",
                  "  console.log('跳过：缺少 ' + _missing.join(', '));",
                  "  if (typeof pm.execution !== \"undefined\" && pm.execution.skipRequest) {",
                  "    pm.execution.skipRequest();",
                  "  }",
                  "}"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const _need = [\"okrOtherRegionBdUserId\"];",
                  "const _missing = _need.filter((k) => !String(pm.environment.get(k) || pm.collectionVariables.get(k) || \"\").trim());",
                  "if (_missing.length) {",
                  "  pm.test('已跳过（缺少 ' + _missing.join(', ') + '）', () => {",
                  "    pm.expect(true).to.be.true;",
                  "  });",
                  "  return;",
                  "}",
                  "pm.test('HTTP 200', () => pm.response.to.have.status(200));",
                  "const body = pm.response.json();",
                  "pm.test('ret_code=20300', () => pm.expect(body.ret_code).to.eql(20300));"
                ]
              }
            }
          ],
          "request": {
            "method": "GET",
            "header": [],
            "url": "{{apiBase}}/api/v1/dashboard/team/okr-progress?manager_id={{okrOtherRegionBdUserId}}"
          }
        },
        {
          "name": "[err] AC-TM-01l 普通 BD 调用",
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const _tok = (pm.environment.get('accessToken') || pm.collectionVariables.get('accessToken') || '').trim();",
                  "if (_tok) {",
                  "  pm.request.headers.upsert({ key: 'Authorization', value: 'Bearer ' + _tok });",
                  "}"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('HTTP 200', () => pm.response.to.have.status(200));",
                  "const body = pm.response.json();",
                  "pm.test('ret_code=20300', () => pm.expect(body.ret_code).to.eql(20300));"
                ]
              }
            }
          ],
          "request": {
            "method": "GET",
            "header": [],
            "url": "{{apiBase}}/api/v1/dashboard/team/okr-progress"
          }
        },
        {
          "name": "[err] AC-TM-01m period 非法",
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const _tok = (pm.environment.get('managerToken') || pm.collectionVariables.get('managerToken') || '').trim();",
                  "if (_tok) {",
                  "  pm.request.headers.upsert({ key: 'Authorization', value: 'Bearer ' + _tok });",
                  "}"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('HTTP 200', () => pm.response.to.have.status(200));",
                  "const body = pm.response.json();",
                  "pm.test('ret_code=10001', () => pm.expect(body.ret_code).to.eql(10001));"
                ]
              }
            }
          ],
          "request": {
            "method": "GET",
            "header": [],
            "url": "{{apiBase}}/api/v1/dashboard/team/okr-progress?period=year"
          }
        },
        {
          "name": "[err] AC-TM-01n 无 Token",
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.request.headers.remove('Authorization');"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('HTTP 200', () => pm.response.to.have.status(200));",
                  "const body = pm.response.json();",
                  "pm.test('ret_code=10007', () => pm.expect(body.ret_code).to.eql(10007));"
                ]
              }
            }
          ],
          "request": {
            "method": "GET",
            "header": [],
            "url": "{{apiBase}}/api/v1/dashboard/team/okr-progress"
          }
        }
      ]
    },
    {
      "name": "B · AC-32-01 GET /dashboard/team/overview",
      "item": [
        {
          "name": "[ok] AC-32-01 本区 Manager 过程卡 + bd_okr",
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const _tok = (pm.environment.get('managerToken') || pm.collectionVariables.get('managerToken') || '').trim();",
                  "if (_tok) {",
                  "  pm.request.headers.upsert({ key: 'Authorization', value: 'Bearer ' + _tok });",
                  "}"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('HTTP 200', () => pm.response.to.have.status(200));",
                  "const body = pm.response.json();",
                  "pm.test('ret_code=0', () => pm.expect(body.ret_code).to.eql(0));",
                  "const r = body.result || {};",
                  "pm.test('team_kpis 过程卡字段', () => {",
                  "  pm.expect(r.team_kpis).to.include.keys('lead_count', 'lead_new_month', 'in_progress_leads', 'offer_stage_leads', 'sla_overdue_leads', 'deal_rate', 'deal_rate_target');",
                  "  pm.expect(r.team_kpis.deal_rate_target).to.eql(null);",
                  "  const dr = Number(r.team_kpis.deal_rate);",
                  "  pm.expect(dr).to.be.at.least(0);",
                  "  pm.expect(dr).to.be.at.most(1);",
                  "});",
                  "pm.test('result.bd_okr 为数组', () => pm.expect(r.bd_okr).to.be.an('array'));",
                  "pm.test('result.bd_okr[] 五卡 signup/eftd/daut/volume/new_affiliates', () => {",
                  "  const metrics = ['signup', 'eftd', 'daut', 'volume', 'new_affiliates'];",
                  "  (r.bd_okr || []).forEach((row) => {",
                  "    pm.expect(row.items).to.be.an('array').and.have.lengthOf(5);",
                  "    pm.expect(row.items.map((it) => it.metric)).to.eql(metrics);",
                  "    row.items.forEach((it) => {",
                  "      if (it.target == null) {",
                  "        pm.expect(it.ratio).to.eql(null);",
                  "      }",
                  "    });",
                  "  });",
                  "});",
                  "pm.test('result.bd_okr[].overall_ratio 在 0–1 或 null', () => {",
                  "  (r.bd_okr || []).forEach((row) => {",
                  "    const v = row.overall_ratio;",
                  "    if (v == null) { return; }",
                  "    pm.expect(Number(v)).to.be.at.least(0);",
                  "    pm.expect(Number(v)).to.be.at.most(1);",
                  "  });",
                  "});",
                  "pm.test('result.bd_okr 与 teamOkrQuarterList 一致', () => {",
                  "  const expected = JSON.parse(pm.collectionVariables.get(\"teamOkrQuarterList\") || 'null');",
                  "  pm.expect(r.bd_okr).to.eql(expected);",
                  "});",
                  "pm.collectionVariables.set(\"teamKpisDefault\", JSON.stringify(r.team_kpis == null ? null : r.team_kpis));"
                ]
              }
            }
          ],
          "request": {
            "method": "GET",
            "header": [],
            "url": "{{apiBase}}/api/v1/dashboard/team/overview"
          }
        },
        {
          "name": "[ok] AC-32-01b period=week 只改 bd_okr",
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const _tok = (pm.environment.get('managerToken') || pm.collectionVariables.get('managerToken') || '').trim();",
                  "if (_tok) {",
                  "  pm.request.headers.upsert({ key: 'Authorization', value: 'Bearer ' + _tok });",
                  "}"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('HTTP 200', () => pm.response.to.have.status(200));",
                  "const body = pm.response.json();",
                  "pm.test('ret_code=0', () => pm.expect(body.ret_code).to.eql(0));",
                  "const r = body.result || {};",
                  "pm.test('result.team_kpis 与 teamKpisDefault 一致', () => {",
                  "  const expected = JSON.parse(pm.collectionVariables.get(\"teamKpisDefault\") || 'null');",
                  "  pm.expect(r.team_kpis).to.eql(expected);",
                  "});",
                  "pm.test('result.bd_okr 与 teamOkrWeekList 一致', () => {",
                  "  const expected = JSON.parse(pm.collectionVariables.get(\"teamOkrWeekList\") || 'null');",
                  "  pm.expect(r.bd_okr).to.eql(expected);",
                  "});"
                ]
              }
            }
          ],
          "request": {
            "method": "GET",
            "header": [],
            "url": "{{apiBase}}/api/v1/dashboard/team/overview?period=week"
          }
        },
        {
          "name": "[err] AC-32-01c 普通 BD 调用",
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const _tok = (pm.environment.get('accessToken') || pm.collectionVariables.get('accessToken') || '').trim();",
                  "if (_tok) {",
                  "  pm.request.headers.upsert({ key: 'Authorization', value: 'Bearer ' + _tok });",
                  "}"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('HTTP 200', () => pm.response.to.have.status(200));",
                  "const body = pm.response.json();",
                  "pm.test('ret_code=20300', () => pm.expect(body.ret_code).to.eql(20300));"
                ]
              }
            }
          ],
          "request": {
            "method": "GET",
            "header": [],
            "url": "{{apiBase}}/api/v1/dashboard/team/overview"
          }
        },
        {
          "name": "[err] AC-32-01d 无 Token",
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.request.headers.remove('Authorization');"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('HTTP 200', () => pm.response.to.have.status(200));",
                  "const body = pm.response.json();",
                  "pm.test('ret_code=10007', () => pm.expect(body.ret_code).to.eql(10007));"
                ]
              }
            }
          ],
          "request": {
            "method": "GET",
            "header": [],
            "url": "{{apiBase}}/api/v1/dashboard/team/overview"
          }
        },
        {
          "name": "[ok] AC-32-01e ADMIN manager_id 范围",
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const _tok = (pm.environment.get('adminToken') || pm.collectionVariables.get('adminToken') || '').trim();",
                  "if (_tok) {",
                  "  pm.request.headers.upsert({ key: 'Authorization', value: 'Bearer ' + _tok });",
                  "}",
                  "const _need = [\"adminToken\",\"okrManagerUserId\"];",
                  "const _missing = _need.filter((k) => !String(pm.environment.get(k) || pm.collectionVariables.get(k) || \"\").trim());",
                  "if (_missing.length) {",
                  "  console.log('跳过：缺少 ' + _missing.join(', '));",
                  "  if (typeof pm.execution !== \"undefined\" && pm.execution.skipRequest) {",
                  "    pm.execution.skipRequest();",
                  "  }",
                  "}"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const _need = [\"adminToken\",\"okrManagerUserId\"];",
                  "const _missing = _need.filter((k) => !String(pm.environment.get(k) || pm.collectionVariables.get(k) || \"\").trim());",
                  "if (_missing.length) {",
                  "  pm.test('已跳过（缺少 ' + _missing.join(', ') + '）', () => {",
                  "    pm.expect(true).to.be.true;",
                  "  });",
                  "  return;",
                  "}",
                  "pm.test('HTTP 200', () => pm.response.to.have.status(200));",
                  "const body = pm.response.json();",
                  "pm.test('ret_code=0', () => pm.expect(body.ret_code).to.eql(0));",
                  "const r = body.result || {};",
                  "pm.test('team_kpis 过程卡字段', () => {",
                  "  pm.expect(r.team_kpis).to.include.keys('lead_count', 'lead_new_month', 'in_progress_leads', 'offer_stage_leads', 'sla_overdue_leads', 'deal_rate', 'deal_rate_target');",
                  "  pm.expect(r.team_kpis.deal_rate_target).to.eql(null);",
                  "  const dr = Number(r.team_kpis.deal_rate);",
                  "  pm.expect(dr).to.be.at.least(0);",
                  "  pm.expect(dr).to.be.at.most(1);",
                  "});",
                  "pm.test('result.bd_okr 为数组', () => pm.expect(r.bd_okr).to.be.an('array'));"
                ]
              }
            }
          ],
          "request": {
            "method": "GET",
            "header": [],
            "url": "{{apiBase}}/api/v1/dashboard/team/overview?manager_id={{okrManagerUserId}}"
          }
        }
      ]
    },
    {
      "name": "C · AC-33-01 GET /dashboard/team/bd-progress",
      "item": [
        {
          "name": "[ok] AC-33-01 本区 Manager 默认 30d",
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const _tok = (pm.environment.get('managerToken') || pm.collectionVariables.get('managerToken') || '').trim();",
                  "if (_tok) {",
                  "  pm.request.headers.upsert({ key: 'Authorization', value: 'Bearer ' + _tok });",
                  "}"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('HTTP 200', () => pm.response.to.have.status(200));",
                  "const body = pm.response.json();",
                  "pm.test('ret_code=0', () => pm.expect(body.ret_code).to.eql(0));",
                  "const r = body.result || {};",
                  "pm.test('result.range=30d', () => pm.expect(r.range).to.eql(\"30d\"));",
                  "pm.test('result.list[] 含 bd_user_id/name/manager_name/leads_count/created_count/follow_up_ontime_rate/active_rate/active_count/affiliate_count/campaign_link_count/total_signup/total_deposit/total_ftd/total_volume/overall_ratio/children', () => {",
                  "  pm.expect(r.list).to.be.an('array');",
                  "  (r.list || []).forEach((it) => {",
                  "    pm.expect(it).to.include.keys(\"bd_user_id\", \"name\", \"manager_name\", \"leads_count\", \"created_count\", \"follow_up_ontime_rate\", \"active_rate\", \"active_count\", \"affiliate_count\", \"campaign_link_count\", \"total_signup\", \"total_deposit\", \"total_ftd\", \"total_volume\", \"overall_ratio\", \"children\");",
                  "  });",
                  "});",
                  "pm.test('result.list[] login_count/campaign_pv/campaign_uv 为 null', () => {",
                  "  (r.list || []).forEach((row) => {",
                  "    pm.expect(row.login_count).to.eql(null);",
                  "    pm.expect(row.campaign_pv).to.eql(null);",
                  "    pm.expect(row.campaign_uv).to.eql(null);",
                  "  });",
                  "});",
                  "pm.test('代理树仅 affiliate + 一层 sub_affiliate，无直客汇总', () => {",
                  "  (r.list || []).forEach((row) => {",
                  "    pm.expect(row.children).to.be.an('array');",
                  "    (row.children || []).forEach((node) => {",
                  "      pm.expect(node.node_type).to.eql('affiliate');",
                  "      pm.expect(String(node.name || '')).to.not.eql('直客汇总');",
                  "      pm.expect(node.total_rebate_fee).to.eql(null);",
                  "      pm.expect(node).to.include.keys(\"affiliate_id\", \"name\", \"sub_affiliate_count\");",
                  "      (node.children || []).forEach((sub) => {",
                  "        pm.expect(sub.node_type).to.eql('sub_affiliate');",
                  "        pm.expect(String(sub.name || '')).to.not.eql('直客汇总');",
                  "        pm.expect(sub.total_rebate_fee).to.eql(null);",
                  "        pm.expect((sub.children || []).length).to.eql(0);",
                  "      });",
                  "    });",
                  "  });",
                  "});",
                  "pm.test('result.list[].overall_ratio 对齐 teamOkrQuarterList', () => {",
                  "  const okr = JSON.parse(pm.collectionVariables.get(\"teamOkrQuarterList\") || '[]') || [];",
                  "  const byId = {};",
                  "  okr.forEach((it) => { byId[String(it.bd_user_id)] = it.overall_ratio; });",
                  "  (r.list || []).forEach((row) => {",
                  "    pm.expect(row.overall_ratio).to.eql(byId[String(row.bd_user_id)]);",
                  "  });",
                  "});"
                ]
              }
            }
          ],
          "request": {
            "method": "GET",
            "header": [],
            "url": "{{apiBase}}/api/v1/dashboard/team/bd-progress"
          }
        },
        {
          "name": "[ok] AC-33-01b range=7d",
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const _tok = (pm.environment.get('managerToken') || pm.collectionVariables.get('managerToken') || '').trim();",
                  "if (_tok) {",
                  "  pm.request.headers.upsert({ key: 'Authorization', value: 'Bearer ' + _tok });",
                  "}"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('HTTP 200', () => pm.response.to.have.status(200));",
                  "const body = pm.response.json();",
                  "pm.test('ret_code=0', () => pm.expect(body.ret_code).to.eql(0));",
                  "const r = body.result || {};",
                  "pm.test('result.range=7d', () => pm.expect(r.range).to.eql(\"7d\"));",
                  "pm.test('result.list 为数组', () => pm.expect(r.list).to.be.an('array'));"
                ]
              }
            }
          ],
          "request": {
            "method": "GET",
            "header": [],
            "url": "{{apiBase}}/api/v1/dashboard/team/bd-progress?range=7d"
          }
        },
        {
          "name": "[ok] AC-33-01c range=90d",
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const _tok = (pm.environment.get('managerToken') || pm.collectionVariables.get('managerToken') || '').trim();",
                  "if (_tok) {",
                  "  pm.request.headers.upsert({ key: 'Authorization', value: 'Bearer ' + _tok });",
                  "}"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('HTTP 200', () => pm.response.to.have.status(200));",
                  "const body = pm.response.json();",
                  "pm.test('ret_code=0', () => pm.expect(body.ret_code).to.eql(0));",
                  "const r = body.result || {};",
                  "pm.test('result.range=90d', () => pm.expect(r.range).to.eql(\"90d\"));"
                ]
              }
            }
          ],
          "request": {
            "method": "GET",
            "header": [],
            "url": "{{apiBase}}/api/v1/dashboard/team/bd-progress?range=90d"
          }
        },
        {
          "name": "[ok] AC-33-01d begin/end 覆盖 range",
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const _tok = (pm.environment.get('managerToken') || pm.collectionVariables.get('managerToken') || '').trim();",
                  "if (_tok) {",
                  "  pm.request.headers.upsert({ key: 'Authorization', value: 'Bearer ' + _tok });",
                  "}"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('HTTP 200', () => pm.response.to.have.status(200));",
                  "const body = pm.response.json();",
                  "pm.test('ret_code=0', () => pm.expect(body.ret_code).to.eql(0));",
                  "const r = body.result || {};",
                  "pm.test('result.begin=2026-08-01', () => pm.expect(r.begin).to.eql(\"2026-08-01\"));",
                  "pm.test('result.end=2026-08-28', () => pm.expect(r.end).to.eql(\"2026-08-28\"));",
                  "pm.test('result.range 为 null', () => pm.expect(r.range).to.eql(null));"
                ]
              }
            }
          ],
          "request": {
            "method": "GET",
            "header": [],
            "url": "{{apiBase}}/api/v1/dashboard/team/bd-progress?range=7d&begin=2026-08-01&end=2026-08-28"
          }
        },
        {
          "name": "[err] AC-33-01e 普通 BD 调用",
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const _tok = (pm.environment.get('accessToken') || pm.collectionVariables.get('accessToken') || '').trim();",
                  "if (_tok) {",
                  "  pm.request.headers.upsert({ key: 'Authorization', value: 'Bearer ' + _tok });",
                  "}"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('HTTP 200', () => pm.response.to.have.status(200));",
                  "const body = pm.response.json();",
                  "pm.test('ret_code=20300', () => pm.expect(body.ret_code).to.eql(20300));"
                ]
              }
            }
          ],
          "request": {
            "method": "GET",
            "header": [],
            "url": "{{apiBase}}/api/v1/dashboard/team/bd-progress"
          }
        },
        {
          "name": "[err] AC-33-01f 无 Token",
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.request.headers.remove('Authorization');"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('HTTP 200', () => pm.response.to.have.status(200));",
                  "const body = pm.response.json();",
                  "pm.test('ret_code=10007', () => pm.expect(body.ret_code).to.eql(10007));"
                ]
              }
            }
          ],
          "request": {
            "method": "GET",
            "header": [],
            "url": "{{apiBase}}/api/v1/dashboard/team/bd-progress"
          }
        },
        {
          "name": "[ok] AC-33-01g ADMIN manager_id 范围",
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const _tok = (pm.environment.get('adminToken') || pm.collectionVariables.get('adminToken') || '').trim();",
                  "if (_tok) {",
                  "  pm.request.headers.upsert({ key: 'Authorization', value: 'Bearer ' + _tok });",
                  "}",
                  "const _need = [\"adminToken\",\"okrManagerUserId\"];",
                  "const _missing = _need.filter((k) => !String(pm.environment.get(k) || pm.collectionVariables.get(k) || \"\").trim());",
                  "if (_missing.length) {",
                  "  console.log('跳过：缺少 ' + _missing.join(', '));",
                  "  if (typeof pm.execution !== \"undefined\" && pm.execution.skipRequest) {",
                  "    pm.execution.skipRequest();",
                  "  }",
                  "}"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const _need = [\"adminToken\",\"okrManagerUserId\"];",
                  "const _missing = _need.filter((k) => !String(pm.environment.get(k) || pm.collectionVariables.get(k) || \"\").trim());",
                  "if (_missing.length) {",
                  "  pm.test('已跳过（缺少 ' + _missing.join(', ') + '）', () => {",
                  "    pm.expect(true).to.be.true;",
                  "  });",
                  "  return;",
                  "}",
                  "pm.test('HTTP 200', () => pm.response.to.have.status(200));",
                  "const body = pm.response.json();",
                  "pm.test('ret_code=0', () => pm.expect(body.ret_code).to.eql(0));",
                  "const r = body.result || {};",
                  "pm.test('result.list 为数组', () => pm.expect(r.list).to.be.an('array'));",
                  "pm.test('代理树仅 affiliate + 一层 sub_affiliate，无直客汇总', () => {",
                  "  (r.list || []).forEach((row) => {",
                  "    pm.expect(row.children).to.be.an('array');",
                  "    (row.children || []).forEach((node) => {",
                  "      pm.expect(node.node_type).to.eql('affiliate');",
                  "      pm.expect(String(node.name || '')).to.not.eql('直客汇总');",
                  "      pm.expect(node.total_rebate_fee).to.eql(null);",
                  "      pm.expect(node).to.include.keys(\"affiliate_id\", \"name\", \"sub_affiliate_count\");",
                  "      (node.children || []).forEach((sub) => {",
                  "        pm.expect(sub.node_type).to.eql('sub_affiliate');",
                  "        pm.expect(String(sub.name || '')).to.not.eql('直客汇总');",
                  "        pm.expect(sub.total_rebate_fee).to.eql(null);",
                  "        pm.expect((sub.children || []).length).to.eql(0);",
                  "      });",
                  "    });",
                  "  });",
                  "});"
                ]
              }
            }
          ],
          "request": {
            "method": "GET",
            "header": [],
            "url": "{{apiBase}}/api/v1/dashboard/team/bd-progress?manager_id={{okrManagerUserId}}"
          }
        }
      ]
    },
    {
      "name": "D · AC-35-01 GET /dashboard/team/affiliates",
      "item": [
        {
          "name": "[ok] AC-35-01 本区 Manager 默认分页",
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const _tok = (pm.environment.get('managerToken') || pm.collectionVariables.get('managerToken') || '').trim();",
                  "if (_tok) {",
                  "  pm.request.headers.upsert({ key: 'Authorization', value: 'Bearer ' + _tok });",
                  "}"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('HTTP 200', () => pm.response.to.have.status(200));",
                  "const body = pm.response.json();",
                  "pm.test('ret_code=0', () => pm.expect(body.ret_code).to.eql(0));",
                  "const r = body.result || {};",
                  "pm.test('分页结构', () => {",
                  "  pm.expect(r).to.include.keys(\"list\", \"total\", \"page\", \"page_size\", \"range\");",
                  "  pm.expect(r.list).to.be.an('array');",
                  "});",
                  "pm.test('result.page=1', () => pm.expect(r.page).to.eql(1));",
                  "pm.test('result.page_size=20', () => pm.expect(r.page_size).to.eql(20));",
                  "pm.test('result.range=30d', () => pm.expect(r.range).to.eql(\"30d\"));",
                  "pm.test('result.list[] 含 affiliate_id/bd_user_id/bd_name/affiliate_source/lead_id', () => {",
                  "  pm.expect(r.list).to.be.an('array');",
                  "  (r.list || []).forEach((it) => {",
                  "    pm.expect(it).to.include.keys(\"affiliate_id\", \"bd_user_id\", \"bd_name\", \"affiliate_source\", \"lead_id\");",
                  "  });",
                  "});",
                  "pm.test('affiliate_source 为 lead|self，lead 必有 lead_id', () => {",
                  "  (r.list || []).forEach((it) => {",
                  "    pm.expect(['lead', 'self']).to.include(it.affiliate_source);",
                  "    if (it.affiliate_source === 'lead') {",
                  "      pm.expect(it.lead_id).to.not.eql(null);",
                  "    } else {",
                  "      pm.expect(it.lead_id).to.eql(null);",
                  "    }",
                  "  });",
                  "});",
                  "if (r?.list?.[0]?.kol_name != null && r?.list?.[0]?.kol_name !== '') {",
                  "  const _v = String(r?.list?.[0]?.kol_name);",
                  "  const _env = (pm.environment.get(\"teamAffiliateKeyword\") || '').toString().trim();",
                  "  const _col = (pm.collectionVariables.get(\"teamAffiliateKeyword\") || '').toString().trim();",
                  "  if (!_col) {",
                  "    pm.collectionVariables.set(\"teamAffiliateKeyword\", _v);",
                  "  }",
                  "  if (!_env) {",
                  "    pm.environment.set(\"teamAffiliateKeyword\", _v);",
                  "  }",
                  "}",
                  "if (r?.list?.[0]?.bd_user_id != null && r?.list?.[0]?.bd_user_id !== '') {",
                  "  const _v = String(r?.list?.[0]?.bd_user_id);",
                  "  const _env = (pm.environment.get(\"okrBdUserId\") || '').toString().trim();",
                  "  const _col = (pm.collectionVariables.get(\"okrBdUserId\") || '').toString().trim();",
                  "  if (!_col) {",
                  "    pm.collectionVariables.set(\"okrBdUserId\", _v);",
                  "  }",
                  "  if (!_env) {",
                  "    pm.environment.set(\"okrBdUserId\", _v);",
                  "  }",
                  "}"
                ]
              }
            }
          ],
          "request": {
            "method": "GET",
            "header": [],
            "url": "{{apiBase}}/api/v1/dashboard/team/affiliates"
          }
        },
        {
          "name": "[ok] AC-35-01b page_size 最大 100",
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const _tok = (pm.environment.get('managerToken') || pm.collectionVariables.get('managerToken') || '').trim();",
                  "if (_tok) {",
                  "  pm.request.headers.upsert({ key: 'Authorization', value: 'Bearer ' + _tok });",
                  "}"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('HTTP 200', () => pm.response.to.have.status(200));",
                  "const body = pm.response.json();",
                  "pm.test('ret_code=0', () => pm.expect(body.ret_code).to.eql(0));",
                  "const r = body.result || {};",
                  "pm.test('result.page_size=100', () => pm.expect(r.page_size).to.eql(100));",
                  "pm.test('result.list.length<=100', () => {",
                  "  pm.expect((r.list || []).length).to.be.at.most(100);",
                  "});"
                ]
              }
            }
          ],
          "request": {
            "method": "GET",
            "header": [],
            "url": "{{apiBase}}/api/v1/dashboard/team/affiliates?page_size=200"
          }
        },
        {
          "name": "[ok] AC-35-01c bd_user_id 只返回该 BD",
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const _tok = (pm.environment.get('managerToken') || pm.collectionVariables.get('managerToken') || '').trim();",
                  "if (_tok) {",
                  "  pm.request.headers.upsert({ key: 'Authorization', value: 'Bearer ' + _tok });",
                  "}",
                  "const _need = [\"okrBdUserId\"];",
                  "const _missing = _need.filter((k) => !String(pm.environment.get(k) || pm.collectionVariables.get(k) || \"\").trim());",
                  "if (_missing.length) {",
                  "  console.log('跳过：缺少 ' + _missing.join(', '));",
                  "  if (typeof pm.execution !== \"undefined\" && pm.execution.skipRequest) {",
                  "    pm.execution.skipRequest();",
                  "  }",
                  "}"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const _need = [\"okrBdUserId\"];",
                  "const _missing = _need.filter((k) => !String(pm.environment.get(k) || pm.collectionVariables.get(k) || \"\").trim());",
                  "if (_missing.length) {",
                  "  pm.test('已跳过（缺少 ' + _missing.join(', ') + '）', () => {",
                  "    pm.expect(true).to.be.true;",
                  "  });",
                  "  return;",
                  "}",
                  "pm.test('HTTP 200', () => pm.response.to.have.status(200));",
                  "const body = pm.response.json();",
                  "pm.test('ret_code=0', () => pm.expect(body.ret_code).to.eql(0));",
                  "const r = body.result || {};",
                  "pm.test('result.list[].bd_user_id', () => {",
                  "  (r.list || []).forEach((it) => {",
                  "    pm.expect(it.bd_user_id).to.eql(Number((pm.environment.get(\"okrBdUserId\") || pm.collectionVariables.get(\"okrBdUserId\") || '')));",
                  "  });",
                  "});"
                ]
              }
            }
          ],
          "request": {
            "method": "GET",
            "header": [],
            "url": "{{apiBase}}/api/v1/dashboard/team/affiliates?bd_user_id={{okrBdUserId}}"
          }
        },
        {
          "name": "[ok] AC-35-01d bd_user_id 团队外为空",
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const _tok = (pm.environment.get('managerToken') || pm.collectionVariables.get('managerToken') || '').trim();",
                  "if (_tok) {",
                  "  pm.request.headers.upsert({ key: 'Authorization', value: 'Bearer ' + _tok });",
                  "}",
                  "const _need = [\"okrOtherRegionBdUserId\"];",
                  "const _missing = _need.filter((k) => !String(pm.environment.get(k) || pm.collectionVariables.get(k) || \"\").trim());",
                  "if (_missing.length) {",
                  "  console.log('跳过：缺少 ' + _missing.join(', '));",
                  "  if (typeof pm.execution !== \"undefined\" && pm.execution.skipRequest) {",
                  "    pm.execution.skipRequest();",
                  "  }",
                  "}"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const _need = [\"okrOtherRegionBdUserId\"];",
                  "const _missing = _need.filter((k) => !String(pm.environment.get(k) || pm.collectionVariables.get(k) || \"\").trim());",
                  "if (_missing.length) {",
                  "  pm.test('已跳过（缺少 ' + _missing.join(', ') + '）', () => {",
                  "    pm.expect(true).to.be.true;",
                  "  });",
                  "  return;",
                  "}",
                  "pm.test('HTTP 200', () => pm.response.to.have.status(200));",
                  "const body = pm.response.json();",
                  "pm.test('ret_code=0', () => pm.expect(body.ret_code).to.eql(0));",
                  "const r = body.result || {};",
                  "pm.test('result.total=0', () => pm.expect(r.total).to.eql(0));",
                  "pm.test('result.list.length<=0', () => {",
                  "  pm.expect((r.list || []).length).to.be.at.most(0);",
                  "});"
                ]
              }
            }
          ],
          "request": {
            "method": "GET",
            "header": [],
            "url": "{{apiBase}}/api/v1/dashboard/team/affiliates?bd_user_id={{okrOtherRegionBdUserId}}"
          }
        },
        {
          "name": "[ok] AC-35-01e keyword 筛选",
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const _tok = (pm.environment.get('managerToken') || pm.collectionVariables.get('managerToken') || '').trim();",
                  "if (_tok) {",
                  "  pm.request.headers.upsert({ key: 'Authorization', value: 'Bearer ' + _tok });",
                  "}",
                  "const _need = [\"teamAffiliateKeyword\"];",
                  "const _missing = _need.filter((k) => !String(pm.environment.get(k) || pm.collectionVariables.get(k) || \"\").trim());",
                  "if (_missing.length) {",
                  "  console.log('跳过：缺少 ' + _missing.join(', '));",
                  "  if (typeof pm.execution !== \"undefined\" && pm.execution.skipRequest) {",
                  "    pm.execution.skipRequest();",
                  "  }",
                  "}"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const _need = [\"teamAffiliateKeyword\"];",
                  "const _missing = _need.filter((k) => !String(pm.environment.get(k) || pm.collectionVariables.get(k) || \"\").trim());",
                  "if (_missing.length) {",
                  "  pm.test('已跳过（缺少 ' + _missing.join(', ') + '）', () => {",
                  "    pm.expect(true).to.be.true;",
                  "  });",
                  "  return;",
                  "}",
                  "pm.test('HTTP 200', () => pm.response.to.have.status(200));",
                  "const body = pm.response.json();",
                  "pm.test('ret_code=0', () => pm.expect(body.ret_code).to.eql(0));",
                  "const r = body.result || {};",
                  "pm.test('result.list 为数组', () => pm.expect(r.list).to.be.an('array'));"
                ]
              }
            }
          ],
          "request": {
            "method": "GET",
            "header": [],
            "url": "{{apiBase}}/api/v1/dashboard/team/affiliates?keyword={{teamAffiliateKeyword}}"
          }
        },
        {
          "name": "[ok] AC-35-01f has_sub=true",
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const _tok = (pm.environment.get('managerToken') || pm.collectionVariables.get('managerToken') || '').trim();",
                  "if (_tok) {",
                  "  pm.request.headers.upsert({ key: 'Authorization', value: 'Bearer ' + _tok });",
                  "}"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('HTTP 200', () => pm.response.to.have.status(200));",
                  "const body = pm.response.json();",
                  "pm.test('ret_code=0', () => pm.expect(body.ret_code).to.eql(0));",
                  "const r = body.result || {};",
                  "pm.test('result.list 为数组', () => pm.expect(r.list).to.be.an('array'));"
                ]
              }
            }
          ],
          "request": {
            "method": "GET",
            "header": [],
            "url": "{{apiBase}}/api/v1/dashboard/team/affiliates?has_sub=true"
          }
        },
        {
          "name": "[ok] AC-35-01g has_sub=false",
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const _tok = (pm.environment.get('managerToken') || pm.collectionVariables.get('managerToken') || '').trim();",
                  "if (_tok) {",
                  "  pm.request.headers.upsert({ key: 'Authorization', value: 'Bearer ' + _tok });",
                  "}"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('HTTP 200', () => pm.response.to.have.status(200));",
                  "const body = pm.response.json();",
                  "pm.test('ret_code=0', () => pm.expect(body.ret_code).to.eql(0));",
                  "const r = body.result || {};",
                  "pm.test('result.list 为数组', () => pm.expect(r.list).to.be.an('array'));"
                ]
              }
            }
          ],
          "request": {
            "method": "GET",
            "header": [],
            "url": "{{apiBase}}/api/v1/dashboard/team/affiliates?has_sub=false"
          }
        },
        {
          "name": "[ok] AC-35-01h affiliate_source=lead",
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const _tok = (pm.environment.get('managerToken') || pm.collectionVariables.get('managerToken') || '').trim();",
                  "if (_tok) {",
                  "  pm.request.headers.upsert({ key: 'Authorization', value: 'Bearer ' + _tok });",
                  "}"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('HTTP 200', () => pm.response.to.have.status(200));",
                  "const body = pm.response.json();",
                  "pm.test('ret_code=0', () => pm.expect(body.ret_code).to.eql(0));",
                  "const r = body.result || {};",
                  "pm.test('result.list[].affiliate_source', () => {",
                  "  (r.list || []).forEach((it) => {",
                  "    pm.expect(it.affiliate_source).to.eql(\"lead\");",
                  "  });",
                  "});"
                ]
              }
            }
          ],
          "request": {
            "method": "GET",
            "header": [],
            "url": "{{apiBase}}/api/v1/dashboard/team/affiliates?affiliate_source=lead"
          }
        },
        {
          "name": "[ok] AC-35-01i affiliate_source=self",
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const _tok = (pm.environment.get('managerToken') || pm.collectionVariables.get('managerToken') || '').trim();",
                  "if (_tok) {",
                  "  pm.request.headers.upsert({ key: 'Authorization', value: 'Bearer ' + _tok });",
                  "}"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('HTTP 200', () => pm.response.to.have.status(200));",
                  "const body = pm.response.json();",
                  "pm.test('ret_code=0', () => pm.expect(body.ret_code).to.eql(0));",
                  "const r = body.result || {};",
                  "pm.test('result.list[].affiliate_source', () => {",
                  "  (r.list || []).forEach((it) => {",
                  "    pm.expect(it.affiliate_source).to.eql(\"self\");",
                  "  });",
                  "});"
                ]
              }
            }
          ],
          "request": {
            "method": "GET",
            "header": [],
            "url": "{{apiBase}}/api/v1/dashboard/team/affiliates?affiliate_source=self"
          }
        },
        {
          "name": "[err] AC-35-01j affiliate_source 非法",
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const _tok = (pm.environment.get('managerToken') || pm.collectionVariables.get('managerToken') || '').trim();",
                  "if (_tok) {",
                  "  pm.request.headers.upsert({ key: 'Authorization', value: 'Bearer ' + _tok });",
                  "}"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('HTTP 200', () => pm.response.to.have.status(200));",
                  "const body = pm.response.json();",
                  "pm.test('ret_code=10001', () => pm.expect(body.ret_code).to.eql(10001));"
                ]
              }
            }
          ],
          "request": {
            "method": "GET",
            "header": [],
            "url": "{{apiBase}}/api/v1/dashboard/team/affiliates?affiliate_source=unknown"
          }
        },
        {
          "name": "[ok] AC-35-01k range=7d 指标窗",
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const _tok = (pm.environment.get('managerToken') || pm.collectionVariables.get('managerToken') || '').trim();",
                  "if (_tok) {",
                  "  pm.request.headers.upsert({ key: 'Authorization', value: 'Bearer ' + _tok });",
                  "}"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('HTTP 200', () => pm.response.to.have.status(200));",
                  "const body = pm.response.json();",
                  "pm.test('ret_code=0', () => pm.expect(body.ret_code).to.eql(0));",
                  "const r = body.result || {};",
                  "pm.test('result.range=7d', () => pm.expect(r.range).to.eql(\"7d\"));"
                ]
              }
            }
          ],
          "request": {
            "method": "GET",
            "header": [],
            "url": "{{apiBase}}/api/v1/dashboard/team/affiliates?range=7d"
          }
        },
        {
          "name": "[err] AC-35-01l 普通 BD 调用",
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const _tok = (pm.environment.get('accessToken') || pm.collectionVariables.get('accessToken') || '').trim();",
                  "if (_tok) {",
                  "  pm.request.headers.upsert({ key: 'Authorization', value: 'Bearer ' + _tok });",
                  "}"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('HTTP 200', () => pm.response.to.have.status(200));",
                  "const body = pm.response.json();",
                  "pm.test('ret_code=20300', () => pm.expect(body.ret_code).to.eql(20300));"
                ]
              }
            }
          ],
          "request": {
            "method": "GET",
            "header": [],
            "url": "{{apiBase}}/api/v1/dashboard/team/affiliates"
          }
        },
        {
          "name": "[err] AC-35-01m 无 Token",
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.request.headers.remove('Authorization');"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('HTTP 200', () => pm.response.to.have.status(200));",
                  "const body = pm.response.json();",
                  "pm.test('ret_code=10007', () => pm.expect(body.ret_code).to.eql(10007));"
                ]
              }
            }
          ],
          "request": {
            "method": "GET",
            "header": [],
            "url": "{{apiBase}}/api/v1/dashboard/team/affiliates"
          }
        },
        {
          "name": "[ok] AC-35-01n ADMIN manager_id 范围",
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const _tok = (pm.environment.get('adminToken') || pm.collectionVariables.get('adminToken') || '').trim();",
                  "if (_tok) {",
                  "  pm.request.headers.upsert({ key: 'Authorization', value: 'Bearer ' + _tok });",
                  "}",
                  "const _need = [\"adminToken\",\"okrManagerUserId\"];",
                  "const _missing = _need.filter((k) => !String(pm.environment.get(k) || pm.collectionVariables.get(k) || \"\").trim());",
                  "if (_missing.length) {",
                  "  console.log('跳过：缺少 ' + _missing.join(', '));",
                  "  if (typeof pm.execution !== \"undefined\" && pm.execution.skipRequest) {",
                  "    pm.execution.skipRequest();",
                  "  }",
                  "}"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const _need = [\"adminToken\",\"okrManagerUserId\"];",
                  "const _missing = _need.filter((k) => !String(pm.environment.get(k) || pm.collectionVariables.get(k) || \"\").trim());",
                  "if (_missing.length) {",
                  "  pm.test('已跳过（缺少 ' + _missing.join(', ') + '）', () => {",
                  "    pm.expect(true).to.be.true;",
                  "  });",
                  "  return;",
                  "}",
                  "pm.test('HTTP 200', () => pm.response.to.have.status(200));",
                  "const body = pm.response.json();",
                  "pm.test('ret_code=0', () => pm.expect(body.ret_code).to.eql(0));",
                  "const r = body.result || {};",
                  "pm.test('分页结构', () => {",
                  "  pm.expect(r).to.include.keys(\"list\", \"total\", \"page\", \"page_size\", \"range\");",
                  "  pm.expect(r.list).to.be.an('array');",
                  "});"
                ]
              }
            }
          ],
          "request": {
            "method": "GET",
            "header": [],
            "url": "{{apiBase}}/api/v1/dashboard/team/affiliates?manager_id={{okrManagerUserId}}"
          }
        }
      ]
    }
  ]
}
